<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <h1>Inicia sesion rellenando el siguiente formulario</h1>
            <br>
            <hr>
            <br>
            <form action="/inicio" method="POST">
                <div class="form-group">
                    <label for="username">Nombre de usuario</label>
                    <input type="text" class="form-control" id="username" name="username">
                </div>
                <div class="form-group">
                    <label for="password">Contraseña</label>
                    <input type="password" class="form-control" id="password" name="pwd">
                </div>
                <button type="submit" class="btn btn-dark btn-block">Iniciar sesion</button>
            </form>
            {{#if errorMsg}}
            <p id="errorMessage">{{errorMsg}}</p>
            {{/if}}
            <hr>
            <p>no estas registrado? <a href="/registrarse">registrate, merluzo</a></p>

            <a href="/">Volver al inicio</a>
        </div>
    </div>
</div>